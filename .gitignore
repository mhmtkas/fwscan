# macOS
.DS_Store
bin/
testdata/images/mini-rootfs.lz4.squashfs
testdata/images/mini-rootfs.zstd.squashfs
testdata/images/mini-rootfs.xz.squashfs
dist/

# The task queue and the phase-0 planning document. Both are working documents
# addressed to whoever is building this rather than to whoever is reading it,
# and the repository is public.
.private/

# The nested spike module builds into its own directory.
spike/vercmp/vercmp

# Editors and agent working directories. Untracked already; listed so a
# `git add -A` in a hurry cannot make them tracked.
.idea/
.vscode/
*.swp
.claude/
